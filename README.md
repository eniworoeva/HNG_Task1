# HNG_Task1
# Go Endpoint Example README

This Go application serves as an example of a simple HTTP endpoint that accepts GET requests and returns specific information in JSON format. The endpoint provides information including Slack name, the current day of the week, current UTC time with validation, track, and a status code of success.

## How to Use

### Prerequisites

Before running this application, make sure you have the following installed:

- Go (Golang): [Download and Install Go](https://golang.org/doc/install)

### Running the Application

1. Clone this repository to your local machine:

   ```bash
   git clone https://github.com/yourusername/your-repo.git
   cd your-repo
